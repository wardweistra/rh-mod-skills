# 007 — Ingest and status skills

Curated skills for existing CLIs. `rh-mod-ingest` reviews sources and PDF tables; `rh-mod-status` is read-only orientation. No new commands.

**Next**: skills authored; CLI unchanged.
