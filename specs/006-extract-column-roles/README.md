# 006 — Extract column roles

Extract plan lists projected columns with roles. Header synonyms are a hint. Reviewer remaps roles in the plan YAML and re-runs `extract plan`.

**Proving**: Dutch NKR still 153/5 from hints; unrecognized CSV remaps without a product patch.
